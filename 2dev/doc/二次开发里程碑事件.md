# 二次开发里程碑事件

## 2026-04-25 | 里程碑：邀请统计充值口径拆分

### 完成事项

- 后端邀请统计新增两套充值口径：
  - `invite_total_recharge_amount`：成功充值订单的站内充值额度汇总
  - `invite_total_recharge_money`：成功充值订单的真实支付金额汇总
- 保留 `invite_total_recharge` 兼容字段，继续表示真实支付金额。
- 前端用户邀请卡片、邀请码列表、被邀请人列表、管理员用户管理“邀请信息”列同步拆分展示：
  - 邀请充值额度
  - 邀请实付金额
  - 邀请消费额度
- `MANUAL-*` 手动绑定码仅用于后台归属统计：
  - 用户侧不展示邀请链接和复制入口
  - 注册接口拒绝使用该类码注册

### 带来的变化

- 管理员和用户可以同时看到“站内充值额度”和“真实支付金额”，避免把 `top_ups.money` 当成站内额度导致统计显示为 `$0.00`。
- 统计关系链路仍然只由 `invite_code_owner_id + invite_code_id > 0` 决定，不改变手动绑定邀请归属的奖励和历史分账边界。

## 2026-04-21 | 里程碑：管理员日志看板 V1

### 完成事项

- 完成管理员日志看板后端能力
  - 新增 `GET /api/log/dashboard?window=1h|6h|24h`
  - 平台总览按 `request_id` 去重统计最终成功 / 最终失败
  - 渠道维度按 `consume/error` 日志尝试统计成功率与失败率
  - 分组维度按最终请求统计成功率与失败率
  - 新增 Top 错误信息、Top 状态码聚合
  - 新增错误信息归一化，去掉 `request id` 动态片段
- 完成管理员日志看板前端页面
  - 新增管理员菜单“日志看板”
  - 新增路由 `/console/log-dashboard`
  - 支持 `1h / 6h / 24h` 切换
  - 支持总览卡、趋势图、渠道统计、分组统计、错误分布展示
  - 默认 60 秒自动刷新，并支持手动刷新
- 完成侧边栏配置接入
  - 管理员 / 根管理员默认侧边栏配置新增 `log_dashboard`
  - “侧边栏模块设置”新增日志看板开关
  - 旧 `SidebarModulesAdmin` 配置加载时自动补齐新 key
- 完成探针排除接入
  - `LogConsumeExcludedUserIDs` 命中用户整体从日志看板排除
  - 健康探针账号不会再拉高日志看板成功率 / 失败率统计
- 完成回归与 docker dev 验证
  - `go test ./controller ./model ./service` 通过
  - `go test ./...` 通过
  - `cd web && bun run build` 通过
  - `docker build -t new-api-local:dev .` 通过
  - 本地 `docker-compose-dev` 接口验证通过

### 带来的变化

- 平台获得了一个独立于“使用日志”明细页的运行态看板，管理员可以更快地看清近窗口的总体成功率与渠道健康度。
- 看板同时提供两套不同口径：
- 看板同时提供三套不同口径：
  - 平台视角：最终请求结果
  - 渠道视角：渠道尝试结果
  - 分组视角：最终请求结果
- 聚合分组 fallback、渠道重试、多条 error log 不会再直接把平台错误率统计拉高。
- 健康探针账号被整体排除后，看板更接近真实用户流量质量。

### 备注

- 第一版直接复用现有 `logs` 表，不新增聚合表、不做 migration。
- 第一版只支持 `1h / 6h / 24h` 固定时间窗口，不支持自定义时间范围和多条件筛选。

## 2026-04-19 | 里程碑：管理员邀请码管理与邀请统计 V1

### 完成事项

- 完成新邀请码数据模型建设
  - 新增 `invite_codes` 表
  - `users` 表新增 `invite_code_id`、`invite_code_owner_id`
  - 新旧邀请码体系并行，旧 `aff_code` 继续保留
- 完成密码注册接入
  - 新增 `invite_code` 注册字段
  - 新用户可通过新邀请码绑定归属用户
  - 注册后自动进入指定分组
  - 奖励次数未耗尽时赠送额度
  - 奖励次数耗尽后仍可注册，但不再赠送额度
- 完成管理员邀请码管理页
  - 支持前缀批量生成邀请码
  - 支持编辑、启用/禁用、删除
  - 支持查看单邀请码维度统计
- 完成邀请统计升级
  - 用户侧可查看：
    - 邀请人数
    - 邀请充值额度
    - 邀请实付金额
    - 邀请消费额度
  - 管理员侧可查看：
    - 单邀请码统计
    - 归属用户维度汇总统计
- 完成回归与 docker dev 验证
  - `go test ./controller ./model ./service` 通过
  - `go test ./...` 通过
  - `cd web && bun run build` 通过
  - 本地 `docker-compose-dev` 集成验证通过

### 带来的变化

- 平台获得了一套可运营、可归属、可统计的新邀请码体系，不再受旧用户自生成 `aff_code` 模型限制。
- 管理员可以把邀请码明确绑定到具体用户，并将注册用户投放到指定分组，适合渠道码、活动码、商务归属码等场景。
- 用户侧的邀请卡片从“奖励额度展示”升级为更有业务价值的真实统计展示。
- 旧 `aff_code` 注册逻辑未被破坏，现有历史邀请链路仍然兼容。

### 备注

- 第一版只接入密码注册页，OAuth / 微信等注册入口暂不接入。
- 表结构创建和老表字段新增全部依赖现有启动时 `AutoMigrate`，未引入独立迁移脚本。

## 2026-04-16 | 里程碑：Stripe 充值回调防伪造与防串单

### 完成事项

- 完成 Stripe 充值入口收口
  - `POST /api/user/stripe/pay` 在配置不完整时直接拒绝
  - `POST /api/user/stripe/amount` 在配置不完整时直接拒绝
  - 不再只依赖前端是否展示 Stripe 充值入口
- 完成 Stripe webhook 空密钥防伪造
  - `POST /api/stripe/webhook` 在 `StripeWebhookSecret` 为空时直接返回 `403`
  - 阻断“空 secret 计算签名后伪造 webhook”这条攻击链路
- 完成 Stripe 异步支付语义补齐
  - `checkout.session.completed` 仅在 `payment_status=paid` 时履约
  - 新增 `checkout.session.async_payment_succeeded`
  - 新增 `checkout.session.async_payment_failed`
- 完成 Stripe 防串单能力
  - 新增 Stripe 专属订单变更函数
  - Stripe webhook 只允许处理 `payment_method='stripe'` 的本地订单
  - 不能再将 `USR...` 这类微信/ePay pending 订单错误更新为成功
- 完成测试与 docker dev 联调验证
  - 单元测试覆盖：
    - Stripe 入口配置拦截
    - 空 webhook secret 拒绝
    - 延迟支付等待
    - 非 Stripe 订单串单拒绝
  - 本地 docker dev 完成“改前攻击成功 / 改后攻击失效 / 正向 Stripe paid 成功 / unpaid 保持 pending”全流程验证

### 带来的变化

- 平台不再允许未配置 `StripeWebhookSecret` 的实例暴露可伪造的 Stripe webhook 入口。
- Stripe webhook 不再具备“完成任意 pending 本地订单”的能力，微信/ePay 订单不会再被 Stripe 回调串成成功。
- Stripe 延迟支付方式的处理与官方推荐语义保持一致，避免在支付尚未最终确认时提前给用户入账。
- 本次改动未改变 ePay、Waffo、Creem 正常生产模式下的支付验签与入账语义。

### 备注

- 本次修复范围聚焦 Stripe 充值回调与串单问题，不扩展到 Creem test mode 等其他测试模式治理。
- 已通过本地 `docker-compose-dev` 验证漏洞利用前后差异，可作为后续线上升级前的回归基线。

## 2026-04-14 | 里程碑：健康探针消费日志排除

### 完成事项

- 完成健康探针账号消费日志排除能力
  - 新增全局配置 `LogConsumeExcludedUserIDs`
  - 支持按用户 ID 维护消费日志排除名单
  - 保存时统一做标准化和格式校验，非法值不会落库
- 完成消费日志写入链路拦截
  - `model.RecordConsumeLog` 在写 `logs` 表前判断用户是否命中排除名单
  - 命中时直接跳过消费日志写入，也不再输出对应 `record consume log` 服务端 info 日志
  - 错误日志不受影响，便于继续观察健康探针失败情况
- 完成后台配置入口
  - 在“运营设置 -> 日志设置”新增“排除消费日志的用户 ID”配置项
  - 支持管理员直接维护多个用户 ID
- 完成回归验证
  - `go test ./controller ./model ./service` 通过
  - `cd web && bun run build` 通过
  - 本地 `docker build` 与 `docker-compose-dev` 集成验证通过

### 带来的变化

- 平台可以保留健康探针真实请求能力，同时避免其刷屏“使用日志”和污染基于 `logs.type=2` 的日志统计。
- 排除逻辑是“写入前跳过”，而不是“查询时隐藏”，因此后台列表和统计口径都能保持干净一致。
- 该能力仅针对消费日志生效，不会屏蔽错误日志，后续探针异常排障仍有抓手。

### 备注

- 第一版仅支持按 `user_id` 排除，不支持用户名、用户组或 token 维度排除。
- 第一版不自动清理历史健康探针消费日志；如需清理，继续使用现有日志清理能力。

## 2026-04-09 | 里程碑：聚合分组链路预算解耦

### 完成事项

- 完成聚合分组链路预算语义调整
  - 聚合分组跨真实分组 fallback 不再受系统全局 `RetryTimes` 提前截断
  - 当前真实分组内部仍按现有 `RetryTimes` 控制 priority/channel 重试
  - 聚合链路可按配置顺序完整尝试 `A -> B -> C`
- 保持现有错误判断规则不变
  - 是否继续下一个真实分组，继续沿用聚合分组 `RetryStatusCodes` 与系统现有重试状态码规则
  - `skip-retry`、本地参数错误、前置校验错误仍直接终止
- 增强聚合 fallback 运行时日志
  - 新增“当前真实分组内部重试”日志
  - 保留“跨真实分组 fallback”与“链路耗尽”日志
- 完成回归验证
  - 补齐聚合分组相关单元测试
  - 本地 `go test ./service ./controller ./model` 通过
  - 本地 `docker build` 与 `docker-compose-dev` 启动验证通过
  - 前端 `bun run build` 通过

### 带来的变化

- 配置了 `A -> B -> C` 的聚合分组后，即使系统 `RetryTimes=0`，仍可完整尝试整条真实分组链路。
- `RetryTimes` 的语义被收敛为“当前真实分组内部预算”，更符合聚合分组高可用链路的预期。
- 普通分组与 `auto` 分组保持原有行为，不会因为本次改造被动改变重试语义。
- 聚合分组的运行日志更容易区分“组内重试”和“跨组 fallback”，便于后续线上排障。

### 备注

- 本次改造不调整 Redis 运行态字段结构，继续使用 `active_index / active_group / last_fail_at / last_success_at / last_switch_at`。
- Redis 聚合运行态 TTL 保持现有实现常量配置，目前为 1 天。

## 2026-04-02 | 里程碑：聚合分组 V1 落地

### 完成事项

- 完成“聚合分组”后端能力建设
  - 新增 `aggregate_groups`、`aggregate_group_targets` 两张表
  - 新增聚合分组管理员 CRUD 接口
  - 新增聚合分组运行时状态存储与懒恢复逻辑
- 完成请求分发链路改造
  - token 仍保持单选 `group`
  - token 可绑定聚合分组
  - 请求进入后按聚合分组展开真实分组链路
  - 支持真实分组优先级顺序、失败切换、恢复后优先尝试高优先级组
- 完成计费逻辑改造
  - 聚合分组使用独立倍率
  - 最终 group ratio 固定取聚合分组倍率
  - 不再跟随底层真实分组倍率变化
  - 异步任务结算复用 group ratio 快照
- 完成用户侧可见性收口
  - 企业用户创建 token 时只看到聚合分组
  - `/api/user/self/groups`、`/api/user/models`、`/api/pricing` 不再向企业用户暴露底层真实分组
  - 用户日志继续记录逻辑组，真实命中组仅保留在管理员调试信息中
- 完成前端管理能力
  - 新增“聚合分组”后台独立菜单
  - 新增聚合分组列表页和创建/编辑面板
  - token 页支持聚合分组选择与聚合标签展示
- 完成测试与文档收口
  - 新增聚合分组相关模型、服务、控制器测试
  - 全量 `go test ./...` 通过
  - 前端 `bun run build` 通过
  - 完成 `aggregate-group-design.md` 与 `迭代开发日志.md`

### 带来的变化

- 平台获得了面向企业客户的“逻辑分组层”，可以在不暴露底层真实渠道组的前提下提供稳定线路。
- 后台可以把多个真实分组聚合成一个对外分组，并通过优先级和 fallback 提高可用性。
- 企业价格体系从“底层真实组倍率驱动”切换为“聚合分组独立倍率驱动”，价格更稳定、可控。
- 存量真实分组、channel、ability、auto 逻辑保持兼容，没有改成 token 多组选路，也没有破坏底层渠道结构。
- 后续如果需要继续迭代模型级 fallback、健康度决策、嵌套聚合策略，已经具备清晰的扩展入口。

### 备注

- 本次里程碑采用自动迁移方案。使用本次二次开发镜像启动时，缺失的聚合分组表会自动创建。
- 后续升级镜像时，迁移逻辑以补齐为主，不会因为正常升级而自动删除现有聚合分组表数据。

## 2026-04-11 | 里程碑：聚合分组智能策略 V1

### 完成事项

- 在现有聚合分组链路上新增“智能临时降级”能力
  - 保留原有真实分组顺序、fallback、懒恢复和重试状态码逻辑
  - 仅在“全局智能策略开启 + 当前聚合分组开启智能路由”时生效
  - 不改数据库里的真实分组顺序，只在运行时临时跳过已降级的真实分组
- 新增聚合分组智能策略配置入口
  - 聚合分组表新增 `smart_routing_enabled`
  - 聚合分组 CRUD 接口和前端编辑页同步支持单组开关
  - 聚合分组管理页顶部新增全局策略卡片
  - 全局参数通过 `aggregate_group.*` Option 键持久化
- 新增运行态临时降级状态
  - 按 `聚合分组 + 模型 + 真实分组` 记录连续失败、连续慢请求和临时降级窗口
  - 保留现有 `aggregate_group:state:<group>:<model>` 运行态结构和 1 天 TTL 不变
  - 智能策略运行态使用独立 key，不破坏旧状态结构
- 新增智能策略日志
  - 记录临时降级跳过
  - 记录连续失败触发降级
  - 记录连续慢请求触发降级
  - 记录降级窗口恢复

### 带来的变化

- 聚合分组获得了“软跳过式”运行时临时降级能力，可以在不改后台配置顺序的前提下避开短时间质量差的真实分组。
- 原有聚合分组、普通分组和 `auto` 分组逻辑保持兼容；默认不启用智能策略时行为不变。
- 现有 `abilities` 模型能力过滤仍然保留，因此本次没有额外引入“不支持模型负缓存”，避免系统复杂度继续上升。

### 备注

- 第一版仅支持“全局策略参数 + 单组开关”，不支持单组阈值覆盖。
- 第一版不做复杂评分模型、不做自动改顺序，仅做最小侵入的连续失败/连续慢请求临时降级。

## 2026-04-24 | 里程碑：CPA 图片响应适配

### 完成事项

- 完成 OpenAI 渠道图片响应适配能力建设
  - 在渠道高级设置新增 `图片响应适配器`
  - 配置项落在 `channels.settings.image_response_adapter`
  - 当前支持值：`无`、`CPA`
- 完成 OpenAI 图片响应分支扩展
  - 仅在 `image_response_adapter=cpa` 时启用 CPA 图片响应适配
  - 适配范围限定为 `/v1/images/generations` 与 `/v1/images/edits`
  - 若上游本身已返回标准 OpenAI 图片格式，则保持原样透传
- 完成 CPA 自定义图片响应归一化
  - 将 CPA 常见图片结果映射为 OpenAI 兼容图片响应
  - 统一输出 `created`
  - 统一输出 `data[].url / data[].b64_json / data[].revised_prompt`
  - 透传 `background / output_format / quality / size / usage`
- 完成测试与联调验证
  - 新增 OpenAI 图片适配相关单元测试
  - 本地 `go test ./relay/channel/openai ./relay/channel` 通过
  - 本地 `go test ./...` 通过
  - 前端 `bun run build` 通过
  - 本地 `docker build` 与 `docker-compose-dev` 重建验证通过
  - 使用真实 `channel_id=8` 的 CPA `gpt-image-2` 渠道完成 live 验证

### 带来的变化

- CPA 接入的 `gpt-image-2` 渠道现在可以在不修改客户端协议的前提下，对外继续返回 OpenAI 兼容图片结果。
- Cherry、脚本、OpenAI SDK 等依赖标准图片响应结构的客户端，可以继续按现有 `created + data[]` 协议消费返回值。
- 原生 OpenAI 图片渠道、以及 Ali、Gemini、Jimeng、Replicate 等既有图片转换链路保持兼容，不会因为本次适配被动改变。

### 备注

- 本次里程碑仅覆盖 `/v1/images/generations` 与 `/v1/images/edits`，不包含 `/v1/images/variations`。
- 本次适配通过渠道配置显式开启，不做“自动识别 CPA 上游”。
- 已在本地 dev 环境中用真实 `group=ikun_gpt-image-2`、真实 `channel_id=8` 验证：
  - `POST /v1/images/generations` 返回 200，响应已统一为 OpenAI 图片格式
  - `POST /v1/images/edits` 返回 200，响应已统一为 OpenAI 图片格式

## 2026-04-25 | 里程碑：后台手动绑定邀请归属

### 完成事项

- 完成后台“统计归属绑定”能力
  - 管理员可在用户管理中为用户绑定、重绑或解绑邀请归属
  - 绑定目标支持用户 ID / 用户名搜索
  - 可选择目标邀请人的已有邀请码
  - 未选择邀请码时自动创建或复用 `MANUAL-<owner_user_id>` 手动码
- 完成新邀请码统计字段同步
  - 绑定、重绑时更新 `inviter_id`、`invite_code_owner_id`、`invite_code_id`
  - 解绑时清零上述三个字段
  - 绑定后可进入 `GetInviteStatsByOwnerUserIDs` 和 `GetInviteeSummariesByOwnerUserID` 统计
- 完成审计日志
  - 绑定、重绑、解绑均写 `LogTypeManage`
  - 日志记录旧邀请人、旧统计归属、旧邀请码、新邀请人、新统计归属、新邀请码
- 完成测试和构建验证
  - 新增模型层和控制器测试
  - `go test ./model ./controller` 通过
  - `cd web && bun run build` 通过

### 带来的变化

- 后台可以修正未通过邀请链路注册用户的邀请统计归属。
- 邀请人统计和邀请人明细会按新的统计归属字段生效。
- 手动绑定不会改变奖励和历史账务，避免对存量额度、奖励次数和历史分账产生副作用。

### 备注

- 本能力定位为统计归属修正，不是补发奖励，也不是历史分账。
- 自动 `MANUAL` 手动码的奖励配置固定为 0，本次绑定不会增加 `reward_used_uses`。
