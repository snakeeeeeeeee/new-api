# 聚合分组 V1 迭代开发日志

更新时间：2026-04-09

## 2026-04-21 | 管理员日志看板 V1

- [x] 日志看板后端聚合与接口
  - 实现内容：
    - 新增 `GET /api/log/dashboard?window=1h|6h|24h`
    - 统计窗口支持 `1h / 6h / 24h`
    - 平台总览按 `request_id` 去重
    - 渠道维度按日志尝试统计
    - 分组维度按最终请求统计
    - Top 错误信息支持去掉 `request id` 动态片段后聚合
    - Top 状态码从 `other.status_code` 提取聚合
    - `LogConsumeExcludedUserIDs` 命中用户整体从看板排除
  - 对应测试：
    - `go test ./controller ./model ./service`
  - 结果：通过

- [x] 管理员日志看板前端页面与菜单
  - 实现内容：
    - 新增管理员菜单“日志看板”
    - 新增前端路由 `/console/log-dashboard`
    - 新增 `1h / 6h / 24h` 切换
    - 新增总览卡、趋势图、渠道统计表、分组统计表、Top 错误信息、Top 状态码
    - 默认 60 秒自动刷新，支持手动刷新
    - 后台侧边栏模块设置新增 `log_dashboard`
  - 对应测试：
    - `cd web && bun run build`
  - 结果：通过

- [x] 管理员默认侧边栏配置与兼容补齐
  - 实现内容：
    - 管理员 / 根管理员默认侧边栏配置新增 `log_dashboard`
    - 侧边栏全局配置页加载旧 `SidebarModulesAdmin` 时自动补齐新 key
    - 不影响既有 `aggregate_group / invite_code / subscription` 等管理菜单
  - 对应测试：
    - `cd web && bun run build`
    - `go test ./...`
  - 结果：通过

## 本地 docker dev 集成验证

- [x] `1h` 看板口径验证
  - 执行方式：
    - 在 `postgres-dev` 中种入专用测试数据
    - 包含：
      - 一个请求先打渠道 A 失败，再回退渠道 B 成功
      - 两个最终失败请求
      - 一个 `request_id` 为空的渠道失败
      - 一个健康探针失败请求
    - 使用管理员 access token 调用 `/api/log/dashboard?window=1h`
  - 观测结果：
    - `summary.total_requests=3`
    - `summary.successful_requests=1`
    - `summary.failed_requests=2`
    - 渠道 `logdash-alpha` 为 `3` 次失败尝试
    - 渠道 `logdash-beta` 为 `1` 次成功尝试 + `1` 次失败尝试
    - 分组 `default` 为 `2` 次最终失败请求
    - 分组 `vip` 为 `1` 次最终成功请求
    - 健康探针渠道 `logdash-probe` 未进入统计

- [x] `6h / 24h` 窗口切换验证
  - 执行方式：
    - 分别调用：
      - `/api/log/dashboard?window=6h`
      - `/api/log/dashboard?window=24h`
  - 观测结果：
    - 两个窗口都能正常返回结构化数据
    - 趋势分桶正确展开
    - 总览数据与 `1h` 口径保持一致

- [x] 空窗口返回验证
  - 执行方式：
    - 临时删除这组测试日志
    - 再次调用 `/api/log/dashboard?window=1h`
    - 校验完成后重新插回测试数据
  - 观测结果：
    - `summary.total_requests=0`
    - `channels=[]`
    - `top_error_messages=[]`
    - `top_status_codes=[]`

- [x] 前端路由可达性验证
  - 执行方式：
    - `curl -I http://127.0.0.1:3001/console/log-dashboard`
  - 结果：
    - 返回 `200`
    - 新页面路由已正常由前端接管

## 本次测试命令

- `go test ./controller ./model ./service`
- `go test ./...`
- `cd web && bun run build`
- `docker build -t new-api-local:dev .`
- `docker compose -f docker-compose-dev.yml up -d --no-deps --force-recreate new-api-dev`
- `docker exec postgres-dev psql -U root -d new-api -c '...'`
- `curl -H 'Authorization: Bearer ...' -H 'New-Api-User: 99001' 'http://127.0.0.1:3001/api/log/dashboard?window=1h'`
- `curl -H 'Authorization: Bearer ...' -H 'New-Api-User: 99001' 'http://127.0.0.1:3001/api/log/dashboard?window=6h'`
- `curl -H 'Authorization: Bearer ...' -H 'New-Api-User: 99001' 'http://127.0.0.1:3001/api/log/dashboard?window=24h'`
- `curl -I http://127.0.0.1:3001/console/log-dashboard`

## 本次结论

- 日志看板 V1 已落地，管理员可以独立查看平台最终成功率与渠道尝试健康度
- 看板直接复用现有 `logs` 表，不新增表，不影响现有使用日志能力
- `LogConsumeExcludedUserIDs` 对日志看板整体生效，健康探针不会污染统计
- 本地 docker dev 集成验证通过，且既有后台菜单、使用日志、邀请码、聚合分组等功能未受影响

## 2026-04-19 | 管理员邀请码管理与邀请统计 V1

- [x] 新邀请码数据模型与迁移接入
  - 实现内容：
    - 新增 `invite_codes` 表
    - `users` 表新增 `invite_code_id`、`invite_code_owner_id`
    - 迁移完全接入现有启动时 `AutoMigrate`
    - 不删除、不修改旧 `aff_code` 字段语义
  - 对应测试：
    - `go test ./controller ./model ./service`
  - 结果：通过

- [x] 密码注册接入新邀请码体系
  - 实现内容：
    - 注册新增 `invite_code` 字段
    - 仅密码注册页接入
    - 通过新邀请码注册时：
      - 绑定归属用户
      - 分配指定分组
      - 写入 `invite_code_id / invite_code_owner_id`
      - 次数未耗尽时赠送额度
      - 次数耗尽后仍允许注册，但不再赠送额度
    - 旧 `aff_code` 注册逻辑保持不变
  - 对应测试：
    - `go test ./controller ./model ./service`
    - `go test ./...`
  - 结果：通过

- [x] 邀请统计升级
  - 实现内容：
    - 新增归属用户维度统计：
      - 邀请人数
      - 邀请总充值
      - 邀请总消费
    - 新增单邀请码维度统计
    - 用户侧 `GetSelf` 返回新统计字段
    - 管理员侧 `GetAllUsers / SearchUsers / GetUser` 返回新统计字段
  - 对应测试：
    - `go test ./controller ./model ./service`
  - 结果：通过

- [x] 管理员邀请码管理页
  - 实现内容：
    - 新增管理员菜单“邀请码管理”
    - 新增后台接口：
      - `GET /api/invite_code`
      - `GET /api/invite_code/search`
      - `GET /api/invite_code/:id`
      - `POST /api/invite_code`
      - `PUT /api/invite_code`
      - `DELETE /api/invite_code/:id`
    - 新增前端列表页与创建/编辑弹窗
    - 支持前缀批量生成、编辑、启停、删除
  - 对应测试：
    - `cd web && bun run build`
  - 结果：通过

## 本地 docker dev 集成验证

- [x] 管理员创建前缀邀请码
  - 执行方式：
    - 构建并启动修复版镜像
    - 通过管理员登录态调用 `/api/invite_code`
  - 结果：
    - 成功创建邀请码 `INV-7KFY5J`
    - 成功绑定归属用户 `invite_owner`
    - 目标分组为 `UserGroup-vip`

- [x] 新邀请码注册与奖励次数递减
  - 执行方式：
    - `invited_user_one` 使用邀请码注册
    - `invited_user_two` 使用同一码再次注册
  - 观测结果：
    - 两个用户均成功注册
    - 两人都进入 `UserGroup-vip`
    - 第一位用户获得 `1000000` 邀请额度
    - 第二位用户未获得额度
    - 邀请码 `reward_used_uses=1 / reward_total_uses=1`

- [x] 次数耗尽后仍可注册，但不再赠送额度
  - 观测结果：
    - `invited_user_two` 成功注册
    - `quota=0`
    - `invite_code_id=1`
    - `invite_code_owner_id=9101`

- [x] 用户侧统计验证
  - 执行方式：
    - 登录归属用户 `invite_owner`
    - 调用 `/api/user/self`
    - 配置两位被邀请用户消费与成功充值数据
  - 观测结果：
    - `invite_user_count=2`
    - `invite_total_recharge=19.75`
    - `invite_total_consume=1100000`

- [x] 管理员侧统计验证
  - 执行方式：
    - 调用 `/api/invite_code/?p=1&page_size=10`
    - 调用 `/api/user/9101`
    - 调用 `/api/user/?p=1&page_size=20`
  - 观测结果：
    - 邀请码列表返回单邀请码统计
    - 用户详情返回归属用户统计
    - 用户列表返回归属用户统计

- [x] 旧 `aff_code` 链路兼容验证
  - 执行方式：
    - 使用旧 `aff_code=AFFLEG` 注册 `legacy_aff_user`
  - 观测结果：
    - 注册成功
    - `inviter_id=9101`
    - `invite_code_id=0`
    - `invite_code_owner_id=0`
    - 旧逻辑未被破坏

## 本次测试命令

- `go test ./controller ./model ./service`
- `go test ./...`
- `cd web && bun run build`
- `docker build -t new-api-local:dev .`
- `docker compose -f docker-compose-dev.yml up -d --force-recreate new-api-dev postgres-dev redis-dev`
- `curl -c /tmp/newapi-admin.cookies ... /api/user/login`
- `curl -b /tmp/newapi-admin.cookies -H 'New-Api-User: 9100' ... /api/invite_code/`
- `curl ... /api/user/register`
- `docker exec postgres-dev psql -U root -d new-api -c '...'`

## 本次结论

- 新邀请码体系已独立落地，不会替换旧 `aff_code`
- 新邀请码与旧邀请码可并行存在
- 表和老表字段全部由现有 `AutoMigrate` 在启动时自动处理
- 用户侧和管理员侧都能看到新统计数据
- docker dev 集成验证通过，且旧 `aff_code` 注册逻辑未被破坏

## 2026-04-16 | Stripe 充值漏洞修复与防串单验证

- [x] Stripe 充值入口配置硬拦截
  - 实现内容：
    - `RequestStripePay` 增加完整配置校验：`StripeApiSecret`、`StripeWebhookSecret`、`StripePriceId`
    - `RequestStripeAmount` 同步增加相同校验
    - 不再只依赖前端隐藏 Stripe 充值入口
  - 对应测试：
    - `go test ./controller ./model`
  - 结果：通过

- [x] Stripe webhook 空密钥防伪造
  - 实现内容：
    - `StripeWebhook` 在验签前先判断 `StripeWebhookSecret`
    - 为空时直接返回 `403`
    - 不再允许空 secret 进入 `ConstructEventWithOptions`
  - 对应测试：
    - `go test ./controller ./model`
    - 本地 `docker-compose-dev` 伪造 webhook 攻击回归
  - 结果：通过

- [x] Stripe 异步支付补齐
  - 实现内容：
    - `checkout.session.completed` 新增 `payment_status == paid` 校验
    - 新增 `checkout.session.async_payment_succeeded`
    - 新增 `checkout.session.async_payment_failed`
    - 延迟支付方式在 `payment_status != paid` 时仅记录等待日志，不提前入账
  - 对应测试：
    - `go test ./controller ./model`
    - `docker-compose-dev` 正向 webhook 验证
  - 结果：通过

- [x] Stripe 防串单修复
  - 实现内容：
    - 新增 Stripe 专属模型函数：
      - `RechargeStripe`
      - `ExpireStripeTopUp`
      - `FailStripeTopUp`
    - Stripe webhook 不再直接调用通用 `model.Recharge`
    - Stripe 专属函数强制要求：
      - 订单存在
      - 订单状态为 `pending`
      - `payment_method = stripe`
    - 命中非 Stripe 订单号时直接拒绝，不能修改订单状态、用户额度和 `stripe_customer`
  - 对应测试：
    - `go test ./controller ./model`
    - `go test ./...`
  - 结果：通过

## 本地攻击复现与修复验证

- 改前攻击复现：通过
  - 执行方式：
    - `docker build -t new-api-local:dev .`
    - `docker compose -f docker-compose-dev.yml up -d --force-recreate new-api-dev postgres-dev redis-dev`
    - 直接在 Postgres 种入一笔 `payment_method='微信'`、`trade_no='USR130NOATTACK1776360000'` 的 pending 订单
    - 使用空 secret 计算 Stripe 签名并向 `/api/stripe/webhook` 发送伪造 `checkout.session.completed`
  - 观测结果：
    - 微信订单被错误更新为 `success`
    - 用户 `quota` 从 `0` 增加到 `125000000`
    - 用户 `stripe_customer` 被写成 `cus_attacker`
    - 服务端日志出现 `收到款项：USR130NOATTACK1776360000, 1.00(USD)`

- 改后同样攻击验证：通过
  - 执行方式：
    - 重建修复版镜像并 `docker compose -f docker-compose-dev.yml up -d --force-recreate new-api-dev`
    - 将同一笔 `USR130NOATTACK1776360000` 微信订单重置回 `pending`
    - 发送完全相同的空 secret 伪造 Stripe webhook
  - 观测结果：
    - 接口返回 `403`
    - 微信订单保持 `pending`
    - 用户 `quota` 保持 `0`
    - 用户 `stripe_customer` 为空
    - 日志出现 `Stripe Webhook Secret 未配置，拒绝处理`

- 修复后正向 Stripe webhook 回归：通过
  - 执行方式：
    - 在 `options` 表配置 `StripeWebhookSecret=whsec_integration_test`
    - 重启 `new-api-dev`
    - 种入两笔 `payment_method='stripe'` 的 pending 订单：
      - `ref_integration_paid`
      - `ref_integration_unpaid`
    - 分别发送：
      - `payment_status=paid`
      - `payment_status=unpaid`
  - 观测结果：
    - `ref_integration_paid` 成功更新为 `success`
    - `ref_integration_unpaid` 保持 `pending`
    - 用户 `quota` 变为 `1250000`
    - 用户 `stripe_customer` 更新为 `cus_ref_integration_paid`
    - 服务端日志出现：
      - `Stripe Checkout 支付尚未完成... ref_integration_unpaid`
      - `收到款项：ref_integration_paid, 2.50(USD)`

## 本次测试命令

- `go test ./controller ./model`
- `go test ./...`
- `cd web && bun run build`
- `docker build -t new-api-local:dev .`
- `docker compose -f docker-compose-dev.yml up -d --force-recreate new-api-dev postgres-dev redis-dev`
- `docker compose -f docker-compose-dev.yml up -d --force-recreate new-api-dev`
- `docker compose -f docker-compose-dev.yml restart new-api-dev`
- `docker exec postgres-dev psql -U root -d new-api -c '...'`
- `python - <<'PY' ... crafted Stripe webhook ... PY`

## 本次结论

- 根因确认：
  - Stripe 空 webhook secret 可被伪造
  - 旧版 Stripe webhook 直接调用通用 `Recharge`，导致任意 pending 本地订单都可能被串成成功
- 修复后行为：
  - Stripe webhook 不能再完成、过期或失败任何非 Stripe 本地订单
  - Stripe 延迟支付方式不再在 `completed` 阶段提前入账
  - ePay / Waffo / Creem 正常生产模式逻辑未被本次改动改变

## 执行规则

- 勾选标准：代码完成 + 对应测试通过
- 后端功能点优先使用 Go 单元测试
- 前端页面改动使用构建验证收口：`bun run build`

## 功能清单

- [x] 数据模型与迁移
  - 实现内容：新增 `aggregate_groups`、`aggregate_group_targets` 两张表并接入自动迁移
  - 对应测试：`go test ./model ./service ./controller`
  - 结果：通过

- [x] 聚合分组 CRUD API
  - 实现内容：新增 `/api/aggregate_group` 管理员 CRUD 接口
  - 对应测试：`go test ./controller -run TestCreateAggregateGroupAndList`
  - 结果：通过

- [x] 用户可见分组合并
  - 实现内容：拆分 `可用组` 与 `可见组`，企业用户界面只暴露聚合分组，认证仍兼容存量真实分组 token
  - 对应测试：`go test ./service -run TestVisibleAggregateGroupsAndRatios`
  - 结果：通过

- [x] token 绑定聚合分组
  - 实现内容：token 创建/更新支持聚合分组；隐藏真实分组不可新建，但旧 token 可保留
  - 对应测试：
    - `go test ./controller -run TestAddTokenAllowsVisibleAggregateGroup`
    - `go test ./controller -run TestAddTokenRejectsHiddenRealGroupWhenAggregateVisible`
  - 结果：通过

- [x] 模型列表按聚合分组暴露
  - 实现内容：聚合分组返回目标真实分组模型并集；用户模型列表与 relay 模型列表兼容聚合分组
  - 对应测试：`go test ./service -run TestVisibleAggregateGroupsAndRatios`
  - 结果：通过

- [x] pricing / 可见组映射防泄露
  - 实现内容：`/api/pricing` 返回可见组倍率并把模型启用分组映射为用户可见分组
  - 对应测试：`go test ./service -run TestVisibleAggregateGroupsAndRatios`
  - 结果：通过

- [x] 聚合组选路状态机
  - 实现内容：请求进入后按聚合分组 targets 顺序展开真实分组链并记录运行态
  - 对应测试：`go test ./service -run TestCacheGetRandomSatisfiedChannelUsesAggregateFallbackAndRecovery`
  - 结果：通过

- [x] 失败切换
  - 实现内容：复用现有 retry 主循环，在真实分组链路中继续 fallback
  - 对应测试：`go test ./service -run TestCacheGetRandomSatisfiedChannelUsesAggregateFallbackAndRecovery`
  - 结果：通过

- [x] 懒恢复
  - 实现内容：运行时状态按 `聚合分组 + 模型` 存储；恢复间隔到期后重新优先尝试高优先级真实分组
  - 对应测试：`go test ./service -run TestCacheGetRandomSatisfiedChannelUsesAggregateFallbackAndRecovery`
  - 结果：通过

- [x] 聚合倍率覆盖计费
  - 实现内容：新增统一 group ratio 解析；聚合分组倍率覆盖最终计费倍率
  - 对应测试：`go test ./service -run TestVisibleAggregateGroupsAndRatios`
  - 结果：通过

- [x] 任务结算适配
  - 实现内容：异步任务差额结算优先使用 `TaskBillingContext.GroupRatio` 快照
  - 对应测试：`go test ./service`
  - 结果：通过

- [x] 日志与管理员调试信息
  - 实现内容：用户日志保留逻辑组；管理员调试信息追加 `aggregate_group` / `route_group`
  - 对应测试：`go test ./...`
  - 结果：通过

- [x] 聚合分组级状态码重试规则
  - 实现内容：聚合分组新增 `retry_status_codes` 配置；留空时沿用系统规则，非空时按聚合分组自定义状态码范围决定是否继续 `A -> B -> C`；同时修复 retry 时未推进到下一个真实分组的 bug，并补充 fallback 链路日志
  - 对应测试：
    - `go test ./service ./controller ./model ./setting/operation_setting`
    - `cd web && bun run build`
  - 结果：通过
  - 完成时间：2026-04-05 14:36:08

- [x] 前端菜单与列表页
  - 实现内容：新增独立“聚合分组”菜单页与管理列表
  - 对应测试：`cd web && bun run build`
  - 结果：通过

- [x] token 页聚合分组选择
  - 实现内容：token 分组下拉展示聚合分组标签；历史隐藏真实分组可保留
  - 对应测试：`cd web && bun run build`
  - 结果：通过

- [x] 回归测试收口
  - 实现内容：补齐全量 Go 回归与前端构建验证；同时修复 `relay/helper` 流扫描测试中的全局超时保护问题
  - 对应测试：
    - `go test ./...`
    - `cd web && bun run build`
  - 结果：通过

## 最终验证

- `go test ./model ./service ./controller`：通过
- `go test ./...`：通过
- `cd web && bun run build`：通过

## 2026-04-05 补充迭代

- [x] 聚合分组 fallback bug 修复与重试规则增强
  - 背景：线上观察到聚合分组首个真实组 `401` 后未切换到下一个真实组，实际 retry 可能仍回到原真实组
  - 修复内容：
    - 新增 `ContextKeyAggregateRetryIndex`
    - 上游返回可重试失败后，下一次 retry 从下一个真实组继续
    - 切到新真实组后，从该真实组自己的最高优先级重新选路
    - 新增聚合分组级 `retry_status_codes` 配置
    - 新增可读 fallback 运行时日志
  - 对应测试：
    - `go test ./service ./controller ./model ./setting/operation_setting`
    - `cd web && bun run build`
  - 结果：通过

- [x] 聚合分组与 channel affinity 冲突修复
  - 背景：命中 `channel affinity`（如内置 `claude cli trace`）且 `skip_retry_on_failure=true` 时，会提前短路 retry，导致聚合分组无法继续真实组 fallback
  - 修复内容：
    - 普通分组保持原有 affinity 行为不变
    - 仅当当前请求为聚合分组时，忽略 `channel affinity` 的 `skip_retry_on_failure`
    - 这样聚合分组仍可在真实组链路内继续高可用切换
  - 对应测试：
    - `go test ./service ./controller ./model ./setting/operation_setting`
  - 结果：通过

- [x] 聚合分组完整交叉测试
  - 实现内容：
    - 验证单分组 `kiro1` / `kiro2`
    - 验证聚合分组 `HA(kiro,kiro)` 正常命中、真实组顺序切换、真实 fallback
    - 验证修改聚合真实组顺序后默认命中变化
    - 验证组内多渠道 direct retry
    - 验证聚合分组在当前真实组存在更低优先级备渠道时，优先在当前真实组内重试，再决定是否切下一个真实组
  - 对应测试：
    - `go test ./service ./controller ./model ./setting/operation_setting`
    - `docker build --progress=plain -t new-api-local:dev .`
    - `docker compose -f docker-compose-dev.yml up -d --force-recreate new-api-dev`
    - 多轮 `curl --globoff ... /v1/messages?beta=true`
    - 多轮 `curl --globoff ... /v1/chat/completions`
    - `docker exec postgres-dev psql -U root -d new-api -c 'select ... from logs ...'`
    - `docker exec redis-dev redis-cli FLUSHALL`
  - 结果：通过
  - 关键结论：
    - 单分组 `kiro2` 成功链路正常；单分组 `kiro1` 在本地禁用状态下按原逻辑直接失败，不会误切其它组
    - 聚合分组在 `kiro1` 本地无可用渠道时，直接命中 `kiro2`
    - 聚合分组在 `kiro1` 上游返回 `503` 时，按配置执行 `kiro1 -> kiro2` fallback 成功
    - 聚合分组顺序改成 `kiro2 -> kiro1` 后，默认命中切换为 `kiro2`
    - 真实组 direct 多渠道可实现 `5 -> 7` 组内重试成功
    - 修复后聚合分组在 `kiro1` 内部也能先走 `5 -> 7`，不会提前跳到 `kiro2`

- [x] 模型广场健康度展示接入
  - 实现内容：
    - 新增后端代理接口 `/api/health/dashboard`
    - 后端从 `https://xxx/health-status/api/dashboard` 拉取健康度数据并做短缓存
    - 模型广场卡片在选中具体分组时展示健康度、平均延迟、状态和简短历史条
    - 模型详情侧边栏增加健康监测面板
  - 对应测试：
    - `go test ./service ./controller ./model ./setting/operation_setting`
    - `cd web && bun run build`
  - 结果：通过

## 联调与异常验证清单

- [x] 普通分组 token 主链路验证
  - 目标：确认普通分组未被聚合分组改造影响
  - 验证点：调用成功、原始日志逻辑组为 `default`、token `used_quota` 增长
  - 执行命令：
    - `python3 2dev/script/test_group_routing.py --base-url http://localhost:3001 --postgres-container postgres-dev --normal-token normaltokentestkey1234567890abcdefghijklmno1234 --normal-model claude-sonnet-4-6 --normal-expected-group default --aggregate-token ...`
  - 结果：通过
  - 观测结果：普通 token `普通分组测试` 请求成功，原始日志 `group=default`，数据库内 `used_quota` 从 `156` 增长到 `204`

- [x] 聚合分组 token 主链路验证
  - 目标：确认聚合分组调用成功
  - 验证点：调用成功、原始日志逻辑组为聚合分组、`admin_info.route_group` 为真实命中组、token `used_quota` 增长
  - 执行命令：
    - `python3 2dev/script/test_group_routing.py --base-url http://localhost:3001 --postgres-container postgres-dev --normal-token ... --aggregate-token dpq7H9zd8XA2aThELCfFsgpx90SxQdRQbf4uKnkrGH2jdKtg --aggregate-model claude-sonnet-4-6 --aggregate-expected-group cc企业专用分组 --aggregate-expected-route-group cc-re`
  - 结果：通过
  - 观测结果：聚合 token `企业聚合` 请求成功，原始日志 `group=cc企业专用分组`，`admin_info.route_group=cc-re`

- [x] 聚合分组强制 fallback 验证
  - 目标：第一优先级真实组不可用时切到第二优先级真实组
  - 验证点：调用成功、`admin_info.route_group` 切换到第二真实组
  - 执行命令：
    - `docker exec postgres-dev psql -U root -d new-api -c "update channels set status = 2 where id = 3; update abilities set enabled = false where channel_id = 3;"`
    - `docker compose -f docker-compose-dev.yml restart new-api-dev`
    - `python3 2dev/script/test_group_routing.py --base-url http://localhost:3001 --postgres-container postgres-dev --normal-token ... --aggregate-token ... --aggregate-expected-route-group cc-max`
  - 结果：通过
  - 观测结果：聚合 token 请求成功，原始日志 `admin_info.route_group=cc-max`

- [x] 聚合分组懒恢复验证
  - 目标：恢复间隔后重新优先尝试高优先级真实组
  - 验证点：先降级成功，再恢复高优先级组后等待恢复间隔，请求重新命中第一真实组
  - 执行命令：
    - `docker exec postgres-dev psql -U root -d new-api -c "update channels set status = 1 where id = 3; update abilities set enabled = true where channel_id = 3;"`
    - `docker compose -f docker-compose-dev.yml restart new-api-dev`
    - `sleep 12 && python3 2dev/script/test_group_routing.py --base-url http://localhost:3001 --postgres-container postgres-dev --normal-token ... --aggregate-token ... --aggregate-expected-route-group cc-re`
  - 结果：通过
  - 观测结果：恢复间隔后聚合 token 请求成功，原始日志 `admin_info.route_group=cc-re`

- [x] 非 fallback 异常验证
  - 目标：非 retryable 错误不应切换真实组
  - 验证点：请求失败，且不出现切到下一真实组的迹象
  - 执行命令：
    - `docker exec postgres-dev psql -U root -d new-api -t -A -c "select coalesce(max(id),0) from logs where token_id = 1;"`
    - `curl -sS -o /tmp/agg_invalid_resp.json -w '%{http_code}' -X POST http://localhost:3001/v1/chat/completions -H 'Authorization: Bearer sk-dpq7H9zd8XA2aThELCfFsgpx90SxQdRQbf4uKnkrGH2jdKtg' -H 'Content-Type: application/json' -d '{"messages":[{"role":"user","content":"hello"}]}'`
    - `docker exec postgres-dev psql -U root -d new-api -t -A -c "select coalesce(max(id),0) from logs where token_id = 1;"`
  - 结果：通过
  - 观测结果：接口返回 `400`，错误为 `Model name not specified`，聚合 token 对应日志最大 `id` 前后保持 `19`，未触发路由切换副作用

- [x] 模型重定向场景验证
  - 目标：聚合分组在 fallback 到第二真实组时，仍可通过渠道“模型重定向”命中真实上游模型
  - 验证点：请求公共模型名成功、`route_group=cc-max`、原始日志包含 `is_model_mapped=true` 和 `upstream_model_name`
  - 执行命令：
    - `docker exec postgres-dev psql -U root -d new-api -c "update channels set status = case when id = 3 then 2 when id = 4 then 1 else status end where id in (3,4); update abilities set enabled = false where channel_id = 3; update abilities set enabled = true where channel_id = 4;"`
    - `docker compose -f docker-compose-dev.yml restart new-api-dev`
    - `python3 2dev/script/test_group_routing.py --base-url http://localhost:3001 --postgres-container postgres-dev --normal-token ... --normal-model claude-sonnet-4-6 --aggregate-token ... --aggregate-model claude-haiku-4-5 --aggregate-expected-group cc企业专用分组 --aggregate-expected-route-group cc-max`

## 2026-04-14 补充迭代

- [x] 健康探针账号消费日志排除
  - 实现内容：
    - 新增全局 Option：`LogConsumeExcludedUserIDs`
    - 支持英文逗号分隔用户 ID，保存前做去空格、去重、正整数校验和标准化
    - 在 `model.RecordConsumeLog` 写入前按 `user_id` 命中排除名单直接跳过
    - 仅影响消费日志；错误日志保留
    - 后台“运营设置 -> 日志设置”新增“排除消费日志的用户 ID”配置项
  - 对应测试：
    - `go test ./controller ./model ./service`
    - `cd web && bun run build`
  - 结果：通过

- [x] docker-compose-dev 集成验证
  - 目标：验证被排除账号成功请求后不写 `type=2` 消费日志，普通账号仍正常写入
  - 执行命令：
    - `docker build -t new-api-local:dev .`
    - `docker compose -f docker-compose-dev.yml up -d --force-recreate new-api-dev`
    - `docker exec postgres-dev psql -U root -d new-api -c "...插入 user_id=17/18 的测试用户、token_id=1017/1018 的测试 token，并 upsert option LogConsumeExcludedUserIDs=17..."`
    - `docker compose -f docker-compose-dev.yml restart new-api-dev`
    - `docker exec redis-dev redis-cli FLUSHALL`
    - `curl --globoff -X POST 'http://localhost:3001/v1/messages?beta=true' ... Bearer <excluded-token>`
    - `curl --globoff -X POST 'http://localhost:3001/v1/messages?beta=true' ... Bearer <normal-token>`
    - `docker exec postgres-dev psql -U root -d new-api -c "select user_id, type, count(*) from logs where user_id in (17,18) group by user_id, type order by user_id, type;"`
  - 结果：通过
  - 观测结果：
    - `user_id=17` 的被排除探针账号请求返回 `200`，但 `logs` 表无新增 `type=2` 记录
    - `user_id=18` 的普通账号请求返回 `200`，`logs` 表新增 1 条 `type=2` 记录
    - 容器日志中仅出现 `record consume log: userId=18`，未出现 `userId=17`
    - `docker exec postgres-dev psql -U root -d new-api -c "select id, model_name, \"group\", other from logs where token_id = 1 order by id desc limit 1;"`
  - 结果：通过
  - 观测结果：请求公共模型 `claude-haiku-4-5` 成功，`route_group=cc-max`，日志中出现 `is_model_mapped=true` 与 `upstream_model_name=claude-haiku-4-5-20251001`

- [x] 所有真实组不可用的最终失败路径验证
  - 目标：聚合分组在所有真实组均不可用时应直接失败，不新增成功消费日志
  - 验证点：接口返回 `503 model_not_found`，聚合 token 消费日志最大 `id` 不增长
  - 执行命令：
    - `docker exec postgres-dev psql -U root -d new-api -c "update channels set status = 2 where id in (3,4); update abilities set enabled = false where channel_id in (3,4);"`
    - `docker compose -f docker-compose-dev.yml restart new-api-dev`
    - `curl -sS -o /tmp/agg_all_down.json -w '%{http_code}' -X POST http://localhost:3001/v1/chat/completions -H 'Authorization: Bearer sk-...' -H 'Content-Type: application/json' -d '{"model":"claude-sonnet-4-6","messages":[{"role":"user","content":"hello"}],"max_tokens":16,"stream":false}'`
    - `docker exec postgres-dev psql -U root -d new-api -c "select coalesce(max(id),0) as max_id from logs where token_id = 1;"`
  - 结果：通过
  - 观测结果：接口返回 `503`，错误码 `model_not_found`，聚合 token 对应日志最大 `id` 保持 `21` 不变

- [x] 公共模型名跨真实组统一验证
  - 目标：相同公共模型名 `claude-haiku-4-5` 在 `cc-re` 和 `cc-max` 两条真实组里都可成功调用，其中 `cc-max` 通过模型重定向映射到 `claude-haiku-4-5-20251001`
  - 验证点：
    - `cc-re` 启用时，请求 `claude-haiku-4-5` 成功，`route_group=cc-re`
    - 下线 `cc-re` 后，请求同一个 `claude-haiku-4-5` 成功，`route_group=cc-max`
    - 第二次日志出现 `is_model_mapped=true` 与 `upstream_model_name=claude-haiku-4-5-20251001`
  - 执行命令：
    - `docker exec postgres-dev psql -U root -d new-api -c "update channels set status = 1 where id in (3,4); update abilities set enabled = true where channel_id in (3,4);"`
    - `docker exec redis-dev redis-cli FLUSHALL`
    - `docker compose -f docker-compose-dev.yml restart new-api-dev`
    - `python3 2dev/script/test_group_routing.py --base-url http://localhost:3001 --postgres-container postgres-dev --normal-token ... --normal-model claude-sonnet-4-6 --aggregate-token ... --aggregate-model claude-haiku-4-5 --aggregate-expected-group cc企业专用分组 --aggregate-expected-route-group cc-re`
    - `docker exec postgres-dev psql -U root -d new-api -c "update channels set status = case when id = 3 then 2 when id = 4 then 1 else status end where id in (3,4); update abilities set enabled = false where channel_id = 3; update abilities set enabled = true where channel_id = 4;"`
    - `docker exec redis-dev redis-cli FLUSHALL`
    - `docker compose -f docker-compose-dev.yml restart new-api-dev`
    - `python3 2dev/script/test_group_routing.py --base-url http://localhost:3001 --postgres-container postgres-dev --normal-token ... --normal-model claude-sonnet-4-6 --aggregate-token ... --aggregate-model claude-haiku-4-5 --aggregate-expected-group cc企业专用分组 --aggregate-expected-route-group cc-max`
    - `docker exec postgres-dev psql -U root -d new-api -c "select id, model_name, \"group\", other from logs where token_id = 1 order by id desc limit 1;"`
  - 结果：通过
  - 观测结果：
    - `cc-re` 启用时，同一个公共模型名 `claude-haiku-4-5` 成功命中 `route_group=cc-re`
    - 下线 `cc-re` 后，同一个公共模型名 `claude-haiku-4-5` 成功命中 `route_group=cc-max`
    - 最新日志中包含 `is_model_mapped=true` 与 `upstream_model_name=claude-haiku-4-5-20251001`

- [x] 聚合分组禁用/删除后的 token 行为
  - 目标：聚合分组被禁用或删除后，已绑定该组的 token 不应继续可用，也不应 silent fallback
  - 验证点：
    - 禁用后调用返回 `403`
    - 删除后调用返回 `403`
  - 执行命令：
    - `docker exec postgres-dev psql -U root -d new-api -c "update aggregate_groups set status = 2 where id = 1;"`
    - `curl -sS -o /tmp/agg_disabled.json -w '%{http_code}' -X POST http://localhost:3001/v1/chat/completions -H 'Authorization: Bearer sk-...' ...`
    - `docker exec postgres-dev psql -U root -d new-api -c "insert into aggregate_groups ... name='cc删除测试组' ...; insert into aggregate_group_targets ...; insert into tokens ... group='cc删除测试组' ...;"`
    - `docker exec postgres-dev psql -U root -d new-api -c "delete from aggregate_group_targets ...; delete from aggregate_groups where name='cc删除测试组';"`
    - `curl -sS -o /tmp/agg_deleted.json -w '%{http_code}' -X POST http://localhost:3001/v1/chat/completions -H 'Authorization: Bearer sk-deleteaggtokentest...' ...`
  - 结果：通过
  - 观测结果：
    - 禁用后返回 `403`，错误提示 `无权访问 cc企业专用分组 分组`
    - 删除后返回 `403`，错误提示 `无权访问 cc删除测试组 分组`

- [x] 聚合组倍率覆盖验证
  - 目标：聚合 token 的最终 group ratio 固定取聚合分组倍率，不跟随底层真实组变化
  - 验证点：
    - 命中 `cc-re` 时日志 `group_ratio=3`
    - 命中 `cc-max` 时日志 `group_ratio=3`
    - `cc-max` 命中时仍同时出现模型重定向字段
  - 执行命令：
    - `docker exec postgres-dev psql -U root -d new-api -c "select id, model_name, (other::json->'admin_info'->>'route_group') as route_group, (other::json->>'group_ratio') as group_ratio, (other::json->>'is_model_mapped') as is_model_mapped, (other::json->>'upstream_model_name') as upstream_model_name from logs where token_id = 1 and model_name = 'claude-haiku-4-5' order by id desc limit 6;"`
  - 结果：通过
  - 观测结果：
    - `route_group=cc-re` 的成功日志里 `group_ratio=3`
    - `route_group=cc-max` 的成功日志里 `group_ratio=3`
    - `cc-max` 日志中有 `is_model_mapped=true` 和 `upstream_model_name=claude-haiku-4-5-20251001`

- [x] 用户侧日志不泄露 route_group
  - 目标：用户侧日志接口不返回 `route_group` / `admin_info`
  - 验证点：
    - `/api/log/token` 不包含 `route_group`
    - `/api/log/self` 不包含 `route_group`
  - 执行命令：
    - `curl -sS http://localhost:3001/api/log/token -H 'Authorization: Bearer sk-...' > /tmp/tokenlog.json`
    - `docker exec redis-dev redis-cli FLUSHALL`
    - `curl -sS -c /tmp/admin.cookies -X POST http://localhost:3001/api/user/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"z123456789"}'`
    - `curl -sS -b /tmp/admin.cookies -H 'New-Api-User: 1' http://localhost:3001/api/log/self > /tmp/selflog.json`
  - 结果：通过
  - 观测结果：
    - `/api/log/token` 最新日志 `group=cc企业专用分组`
    - `other` 中不包含 `route_group`
    - `other` 中不包含 `admin_info`
    - `/api/log/self` 最新日志同样不包含 `route_group` 与 `admin_info`

- [x] Redis 保持状态 / Redis 清空后重建状态
  - 目标：聚合分组运行时状态在 Redis 中可保持，清空后可重新按高优先级建立
  - 验证点：
    - 降级到 `cc-max` 后，仅重启应用不清 Redis，请求仍命中 `cc-max`
    - 清空 Redis 后再请求，重新命中 `cc-re`
  - 执行命令：
    - `docker exec postgres-dev psql -U root -d new-api -c "update aggregate_groups set recovery_interval_seconds = 300 where id = 1;"`
    - `docker exec redis-dev redis-cli FLUSHALL`
    - 下线 `cc-re`，保留 `cc-max`，重启应用并调用聚合 token，请求公共模型 `claude-haiku-4-5`
    - 恢复 `cc-re` 但不清 Redis，重启应用，再次请求公共模型 `claude-haiku-4-5`
    - `docker exec redis-dev redis-cli FLUSHALL`
    - 重启应用，再次请求公共模型 `claude-haiku-4-5`
  - 结果：通过
  - 观测结果：
    - 第一次降级后命中 `route_group=cc-max`
    - 恢复 `cc-re` 但不清 Redis 后，仍命中 `route_group=cc-max`
    - 清空 Redis 后重新请求，命中 `route_group=cc-re`

- [x] 不同用户组可见性验证
  - 目标：仅 `visible_user_groups` 包含的用户组能看到聚合分组
  - 验证点：
    - 当前聚合分组 `cc企业专用分组` 的 `visible_user_groups=["svip"]`
    - `svip` 用户能看到聚合分组
    - `default`、`vip` 用户看不到聚合分组
  - 执行命令：
    - 插入临时用户：
      - `temp_default / z123456789 / group=default`
      - `temp_vip / z123456789 / group=vip`
    - 分别登录 `admin`、`temp_default`、`temp_vip`
    - 分别调用 `/api/user/self/groups`
  - 结果：通过
  - 观测结果：
    - `svip(admin)` 返回的 groups 包含 `cc企业专用分组`
    - `default` 返回的 groups 不包含 `cc企业专用分组`
    - `vip` 返回的 groups 不包含 `cc企业专用分组`

## 2026-04-09 补充迭代

- [x] 聚合分组链路预算与组内重试解耦
  - 背景：线上使用 `A -> B -> C` 聚合分组时，跨真实分组 fallback 会被系统全局 `RetryTimes` 提前截断，导致配置了后续真实分组也可能永远尝试不到
  - 实现内容：
    - 聚合分组跨真实分组 fallback 不再受全局 `RetryTimes` 提前截断
    - 当前真实分组内部仍沿用现有 `RetryTimes` 控制 priority/channel 重试
    - 是否继续下一个真实分组，继续沿用现有聚合分组 `RetryStatusCodes` 与系统重试状态码规则
    - 新增聚合“组内重试”与“跨组 fallback”区分日志
  - 对应测试：
    - `go test ./service -run 'AggregateGroup'`
    - `go test ./service ./controller ./model`
  - 结果：通过
  - 关键结论：
    - `RetryTimes = 0` 时，聚合分组仍可继续尝试下一个真实分组
    - `RetryTimes > 0` 时，当前真实分组内部重试次数仍受现有配置控制
    - 普通分组与 `auto` 分组行为保持不变

- [x] 本地 dev 镜像构建与启动验证
  - 实现内容：
    - 使用当前代码重新构建 `new-api-local:dev`
    - 重启 `docker-compose-dev.yml` 中的 `new-api-dev`
    - 验证本地服务状态接口正常响应
  - 对应测试：
    - `docker build -t new-api-local:dev .`
    - `docker compose -f docker-compose-dev.yml up -d --force-recreate new-api-dev`
    - `curl -fsS http://localhost:3001/api/status`
    - `cd web && bun run build`
  - 结果：通过

## 本地联调环境说明

- 本地 dev 环境登录账号：
  - 用户名：`admin`
  - 密码：`z123456789`
- 本地 dev 环境在高频接口联调后，可能触发 Redis 中的 IP 限流，表现为登录接口返回 `429`
- 遇到 `429` 时的清理方式：
  - `docker exec -it redis-dev redis-cli FLUSHALL`
  - `docker compose -f docker-compose-dev.yml restart new-api-dev`
- 本轮联调结束后已执行一次限流清理与应用重启，当前环境为可继续使用状态

## 2026-04-11 补充迭代

- [x] 聚合分组智能策略 V1
  - 背景：
    - 线上聚合分组存在“某真实分组能用但很慢”或“短时间连续失败”的情况
    - 希望在不破坏现有聚合逻辑的前提下，为聚合分组增加最小侵入的智能临时降级能力
  - 实现内容：
    - 聚合分组新增单组开关 `smart_routing_enabled`
    - 聚合分组管理页顶部新增全局策略卡片
    - 新增全局配置：
      - `aggregate_group.smart_strategy_enabled`
      - `aggregate_group.consecutive_failure_threshold`
      - `aggregate_group.degrade_duration_seconds`
      - `aggregate_group.slow_request_threshold_seconds`
      - `aggregate_group.consecutive_slow_threshold`
    - 新增按 `聚合分组 + 模型 + 真实分组` 维度的运行时临时降级状态
    - 仅在“全局开启 + 单组开启”时生效
    - 连续失败达到阈值后，后续请求临时跳过该真实分组
    - 连续慢请求达到阈值后，后续请求临时跳过该真实分组
    - 保留原有真实分组顺序、fallback、懒恢复和重试状态码逻辑
    - 不新增“不支持模型负缓存”，继续沿用现有 `abilities` 模型能力过滤
  - 对应测试：
    - `go test ./service -run 'AggregateGroup'`
    - `go test ./controller -run 'AggregateGroup|Option'`
    - `go test ./service ./controller ./model`
    - `cd web && bun run build`
  - 结果：通过
  - 关键结论：
    - 智能策略关闭时，聚合分组选路与旧逻辑一致
    - 智能策略开启后，仅增加运行时临时降级能力，不会改数据库中的真实分组顺序
    - 慢请求降级只影响后续请求，不会打断当前已成功请求

- [x] 本地 dev Docker 联调验证
  - 实现内容：
    - 重新构建 `new-api-local:dev`
    - 重启 `docker-compose-dev.yml` 中的 `new-api-dev`
    - 使用本地脚本验证新增配置接口与聚合分组更新接口
  - 对应测试：
    - `docker build -t new-api-local:dev .`
    - `docker compose -f docker-compose-dev.yml up -d --force-recreate new-api-dev`
    - `curl -fsS http://localhost:3001/api/status`
    - 登录本地 dev 管理员后调用：
      - `GET /api/option` 校验 `aggregate_group.*` 新键返回
      - `PUT /api/option` 校验全局智能策略参数可保存并立即回读
      - `GET /api/aggregate_group/1` 校验 `smart_routing_enabled` 字段返回
      - `PUT /api/aggregate_group/` 校验单组智能路由开关可更新并回读
  - 结果：通过

- [x] 本地聚合分组智能策略行为验证
  - 验证对象：
    - 真实分组：`claude-re-kiro`、`claude-re-kiro-002`
    - 聚合分组：`kiro1-kiro2-ha`
    - 聚合 token：`mix-kiro1-kiro2`
  - 验证场景：
    - 连续失败临时降级
      - 临时将 `aggregate_group.consecutive_failure_threshold` 调整为 `2`
      - 清空 `claude-opus-4-6` 对应聚合运行态与智能状态
      - 连续请求 `claude-opus-4-6`
      - 观测结果：
        - 前两次请求先走 `claude-re-kiro(channel#5)`，失败后 fallback 到 `claude-re-kiro-002(channel#6)`
        - 第二次后命中 `aggregate smart degrade by consecutive failures`
        - 第三次请求出现 `aggregate smart strategy skipped degraded route: ... route_group=claude-re-kiro`
        - 第三次成功日志中 `use_channel=["6"]`，说明后续请求已直接跳过 `kiro1`
    - 连续慢请求临时降级
      - 临时将 `aggregate_group.slow_request_threshold_seconds=1`
      - 临时将 `aggregate_group.consecutive_slow_threshold=2`
      - 临时将 `aggregate_group.consecutive_failure_threshold=100`，避免失败阈值干扰
      - 连续请求 `claude-opus-4-6`
      - 观测结果：
        - 第一、二次在 `claude-re-kiro-002(channel#6)` 成功但耗时较高
        - 第二次后命中 `aggregate smart degrade by consecutive slow requests`
        - 第三次请求出现 `aggregate smart strategy skipped degraded route: ... route_group=claude-re-kiro-002`
    - 全局关闭回退旧逻辑
      - 将 `aggregate_group.smart_strategy_enabled=false`
      - 清空 `claude-opus-4-6` 对应聚合运行态与智能状态
      - 再次请求聚合 token
      - 观测结果：
        - 仍按旧逻辑走 `claude-re-kiro -> claude-re-kiro-002`
        - 未生成新的 `aggregate_group:smart_state:*` Redis 状态
    - 非聚合分组不受影响
      - 使用直连 token `kiro2`
      - 直接请求 `claude-opus-4-6`
      - 观测结果：
        - 日志不包含任何 `aggregate_*` 相关字段
        - 成功日志中仅记录 `group="claude-re-kiro-002"` 和 `use_channel=["6"]`
  - 收尾恢复：
    - 将全局参数恢复为：
      - `aggregate_group.smart_strategy_enabled=true`
      - `aggregate_group.consecutive_failure_threshold=5`
      - `aggregate_group.slow_request_threshold_seconds=30`
      - `aggregate_group.consecutive_slow_threshold=3`
    - 清理测试过程中写入的 `claude-opus-4-6` 聚合运行态与智能状态
  - 结果：通过

## 2026-04-24 补充迭代

- [x] CPA 图片响应适配
  - 背景：
    - CPA 上游接入的 `gpt-image-2` 返回体与标准 OpenAI 图片返回不完全一致
    - 客户端侧仍希望继续按 OpenAI 图片响应结构消费，不改调用协议
  - 实现内容：
    - 在 OpenAI 渠道高级设置新增 `图片响应适配器`
    - 配置存储于 `channels.settings.image_response_adapter`
    - 当前支持值：`无`、`cpa`
    - OpenAI adaptor 仅在 `image_response_adapter=cpa` 且请求路径为图片接口时启用响应适配
    - 若上游已是标准 OpenAI 图片响应，则保持原样透传
    - 若上游为 CPA 自定义结构，则统一映射到 OpenAI 兼容 `ImageResponse`
    - 统一输出字段：
      - `created`
      - `data[].url / data[].b64_json / data[].revised_prompt`
      - `background / output_format / quality / size / usage`
  - 对应测试：
    - `go test ./relay/channel/openai ./relay/channel`
    - `go test ./...`
    - `cd web && bun run build`
  - 结果：通过
  - 关键结论：
    - `image_response_adapter=""` 时，OpenAI 图片响应行为保持不变
    - `image_response_adapter="cpa"` 时，标准 OpenAI 图片响应仍原样返回
    - `image_response_adapter="cpa"` 时，CPA 自定义图片响应可成功归一化为 OpenAI 图片格式

- [x] 本地 dev Docker 联调验证
  - 实现内容：
    - 使用当前代码重新构建 `new-api-local:dev`
    - 强制重建 `docker-compose-dev.yml` 中的 `new-api-dev`
    - 验证服务健康状态与图片 relay 路径
  - 对应测试：
    - `docker build -t new-api-local:dev .`
    - `docker compose -f docker-compose-dev.yml up -d --no-deps --force-recreate new-api-dev`
    - `curl -fsS http://localhost:3001/api/status`
  - 结果：通过

- [x] 真实 CPA 渠道 live 验证
  - 验证对象：
    - 真实分组：`ikun_gpt-image-2`
    - 真实渠道：`channel_id=8`
    - 验证 token：`token_id=16`
  - 验证场景：
    - `POST /v1/images/generations`
      - 返回 200
      - 命中日志 `channel_id=8`
      - 返回体包含：
        - `created`
        - `data[0].b64_json`
        - `revised_prompt`
        - `background / output_format / quality / size / usage`
    - `POST /v1/images/edits`
      - 返回 200
      - 命中日志 `channel_id=8`
      - 返回体同样统一为 OpenAI 图片响应结构
  - 结果：通过
  - 关键结论：
    - 当前真实 CPA `gpt-image-2` 渠道已可稳定支持 `generations` 与 `edits`
    - 当前仓库中的 `/v1/images/variations` 仍是未实现状态，本次不纳入上线范围

## 2026-04-25 补充迭代

- [x] 后台手动绑定邀请归属
  - 背景：
    - 存量用户可能没有通过邀请链接或邀请码注册，但运营上需要计入某个邀请人的新邀请统计。
    - 新统计逻辑依赖 `invite_code_owner_id` 且要求 `invite_code_id > 0`，只改旧字段 `inviter_id` 不会进入新统计。
  - 实现内容：
    - 新增管理员接口：
      - `PUT /api/user/:id/invite_binding`
      - `DELETE /api/user/:id/invite_binding`
      - `GET /api/user/:id/invite_codes`
    - 绑定和重绑时事务化更新被绑定用户：
      - `inviter_id`
      - `invite_code_owner_id`
      - `invite_code_id`
    - 未指定邀请码时自动创建或复用 `MANUAL-<owner_user_id>` 手动码。
    - 自动手动码奖励配置固定为 0，并保持启用状态。
    - 解绑时清零上述三个邀请归属字段。
    - 每次绑定、重绑、解绑写 `LogTypeManage` 管理日志，记录旧/新邀请人和旧/新邀请码。
    - 用户管理表格操作菜单新增“绑定邀请人”，弹窗支持用户 ID / 用户名搜索、邀请码选择、自动手动码和解绑。
  - 非目标范围：
    - 不补发邀请奖励。
    - 不增加 `invite_codes.reward_used_uses`。
    - 不修改 `aff_quota` / `aff_history` / `aff_count`。
    - 不做历史分账或历史回算。
  - 对应测试：
    - `go test ./model ./controller`
    - `cd web && bun run build`
  - 结果：通过
  - 设计说明：
    - `2dev/doc/manual-invite-binding-design.md`
